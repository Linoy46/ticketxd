import { MinusculasDirective } from './minusculas.directive';

describe('MinusculasDirective', () => {
  it('should create an instance', () => {
    const directive = new MinusculasDirective();
    expect(directive).toBeTruthy();
  });
});
